<!doctype html>
<html lang="{{ request.locale.iso_code }}">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Find Your Dandy Moment</title>
  <meta name="description" content="Take the short Dandy quiz to find the experience that best fits your moment." />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Barlow:wght@400;500;600;700;800&family=Barlow+Condensed:ital,wght@0,700;0,800;0,900;1,800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="{{ 'lp-moment-styles.css' | asset_url }}" />
  <link rel="stylesheet" href="{{ 'lp-moment-enhancements.css' | asset_url }}" />

  <link rel="canonical" href="{{ canonical_url }}">
  <style>img { height: auto; }</style>
  {{ content_for_header }}
</head>
<body>
{{ content_for_layout }}
</body>
</html>
