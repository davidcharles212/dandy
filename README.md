# Dandy landing-page media

Video files served to the storefront through jsDelivr (correct Content-Type and byte-range support), because Shopify theme assets cannot hold video. Tag each release (media-cshop-vN) and reference the tag from the theme section setting `video_base`.
